---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'input'> {}

const { class: className, ...props } = Astro.props
---

<input
  class:list={['border-b border-zinc-400 p-2 outline-0', className]}
  {...props}
/>
